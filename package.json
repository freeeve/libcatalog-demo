{
  "name": "libcat-demo",
  "version": "0.0.0",
  "private": true,
  "description": "Eve's Library -- a public demo of the libcat Hugo module. Static, faceted, Pagefind-searchable catalog.",
  "scripts": {
    "build": "hugo --minify --destination public",
    "search:index": "npx -y pagefind --site public",
    "build:full": "npm run build && npm run search:index",
    "data:refresh": "bash scripts/refresh-data.sh"
  }
}
